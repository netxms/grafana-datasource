apiVersion: 1

datasources:
  - name: 'NetXMS'
    type: 'radensolutions-netxms-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      serverAddress: 'http://netxms-server:8000/'
    secureJsonData:
      apiKey: $NX_API_KEY
